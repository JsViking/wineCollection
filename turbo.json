{
  "$schema": "https://turborepo.org/schema.json",
  "globalDependencies": ["$MONGO_URI", "$API_URL"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
